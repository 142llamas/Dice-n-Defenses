# Firebase (placeholder — NOT used yet)

This folder is intentionally empty of code during Phase 0.

Firebase (Hosting, Authentication, Cloud Firestore) is a LATER phase (Phase 10 in
the Source of Truth). The core game must run, build, and save locally first.

When that phase arrives, this folder will hold:
- firebaseConfig.ts
- authService.ts
- cloudSaveService.ts
- leaderboardService.ts

Never commit Firebase service-account credentials or private keys.
