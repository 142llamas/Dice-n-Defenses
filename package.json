{
  "name": "fantasy-tower-defense",
  "private": true,
  "version": "0.2.0-dev",
  "type": "module",
  "description": "Turn-based grid fantasy wave-defense tactics game with a full D&D-depth character system and a 6-region story campaign. Live at https://dice-n-defenses.web.app.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:rules": "firebase emulators:exec --project demo-fantasy-td --only firestore \"vitest run --config firestore-tests/vitest.config.ts\"",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && firebase deploy --only hosting,firestore:rules"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-tools": "^15.25.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^3.2.4"
  }
}
